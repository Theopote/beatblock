# BeatBlock audio analyzer — required dependencies (basic analysis mode)
librosa>=0.10.1,<0.11
soundfile>=0.12.1,<0.13
numpy>=1.26,<2.0
scipy>=1.11,<1.13

# Optional — Demucs stem separation (--demucs mode):
#   pip install -r requirements-demucs.txt
# Pulls in demucs + torch (heavyweight, includes PyTorch). Python 3.10–3.12 recommended.
# The mod can auto-install these when Demucs is enabled; see requirements-demucs.txt.
